#!/bin/sh

build_dir = "$1"
cache_dir = "$2"
env_dir - "$3"

cd build_dir
echo "Fetching latest Erlang..."
wget -o "$cache_dir/erlang.tar.gz" https://erlang.org/download/otp_src_23.1.tar.gz 
printf "Downloaded Erlang. Size: %d\n" $(wc -c < $cache_dir/erlang.tar.gz)